# Global defaults for build scripts and images

# Timezone
TZ=Asia/Shanghai
DOCKER_PLATFORM=linux/amd64
# Docker registry mirror for base images
# Supported aliases: daocloud, aliyun, tencent, ustc, netease, or custom registry URL
DOCKER_MIRROR=daocloud

# Image tags
MANAGER_API_IMAGE_TAG=xiaozhi-manager-api:latest
SERVER_IMAGE_TAG=xiaozhi-server:latest
WEB_REACT_IMAGE_TAG=manager-web-react:latest

# MySQL settings
DB_HOST=14.103.206.69
DB_PORT=3306
DB_NAME=zj_new_production
DB_USER=zj_new_production
DB_PASS=eTBRda3NcpJ6dmd4

# Redis settings
REDIS_HOST=14.103.206.69
REDIS_PORT=6379
REDIS_PASS=

# Web (React) build options
WEB_REACT_MIRROR=
WEB_REACT_SPA_PATH=main/manager-web-react
WEB_REACT_BUILD_DIR=dist
WEB_REACT_SKIP_BUILD=0

# Ports for local runs
WEB_PORT=8002
SERVER_WS_PORT=8000
SERVER_HTTP_PORT=8003
